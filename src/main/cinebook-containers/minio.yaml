version: "3.8"

services:
  minio:
    image: quay.io/minio/minio:latest
    container_name: cinebook-minio
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=cinebook
      - MINIO_ROOT_PASSWORD=cinebook123
    ports:
      - "9000:9000"   # S3 API
      - "9001:9001"   # Web Console
    restart: unless-stopped

volumes:
  minio_data:
    driver: local